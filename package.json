{
  "name": "11ty-kj",
  "version": "1.0.0",
  "description": "An 11ty blog website",
  "main": ".eleventy.js",
  "scripts": {
    "start": "npx eleventy --serve",
    "production": "npm run clean && npx eleventy",
    "debug": "DEBUG=Eleventy* npx @11ty/eleventy",
    "clean": "rm -rf build",
    "build": "env NODE_ENV=production npx eleventy"
  },

  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "@11ty/eleventy-navigation": "^0.1.6"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0"
  }
}
